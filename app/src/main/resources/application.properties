spring.application.name=app
server.port=${PORT:8080}
app.frontend.url=${FE_URL:https://tmdt-fashion-ssl8-nine.vercel.app/}

spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?sslMode=REQUIRED
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:12345}

spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update

# JWT Configuration
jwt.secret=${JWT_SECRET:ThayTheBangChuoiSecretKeySieuDaiVaBaoMatCuaBanNhe1234567890}
jwt.access-token-expiration=${JWT_ACCESS_EXPIRATION:1800000}
jwt.refresh-token-expiration=${JWT_REFRESH_EXPIRATION:604800000}

# CAU HINH GUI EMAIL (JavaMailSender)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:email_cua_ban@gmail.com}
spring.mail.password=${MAIL_PASSWORD:mat_khau_ung_dung_16_ky_tu}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# VNPAY PAYMENT CONFIGURATION (SANDBOX)
# Lay tai https://sandbox.vnpayment.vn/devreg (dang ky tai khoan test) -> mail nhan duoc vnp_TmnCode + vnp_HashSecret
vnpay.tmn-code=${VNPAY_TMN_CODE:your_tmn_code}
vnpay.hash-secret=${VNPAY_HASH_SECRET:your_hash_secret}
vnpay.pay-url=${VNPAY_PAY_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
vnpay.api-url=${VNPAY_API_URL:https://sandbox.vnpayment.vn/merchant_webapi/api/transaction}
# Return URL phai tro ve BACKEND (khong phai FE) de backend xac thuc chu ky truoc khi redirect nguoi dung ve FE
vnpay.return-url=${VNPAY_RETURN_URL:http://localhost:8080/api/payments/vnpay/return}
vnpay.version=2.1.0
# Luu y: URL nhan IPN (server-to-server) khong truyen qua request tao thanh toan nhu MoMo.
# Phai khai bao thu cong trong VNPay Merchant Admin (Sandbox) -> URL: {vnpay.return-url nhung doi /return thanh /ipn}
# Vi du: http://localhost:8080/api/payments/vnpay/ipn (khi test local can dung ngrok/tunnel vi VNPay goi tu server that)

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dpqadtayu}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}